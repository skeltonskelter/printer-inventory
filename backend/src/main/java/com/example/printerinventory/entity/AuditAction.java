package com.example.printerinventory.entity;

public enum AuditAction {
    CREATE, UPDATE, DELETE, RELOCATE, IMPORT, LOGIN, LOGOUT,
    USER_CREATE, USER_UPDATE, USER_ENABLE, USER_DISABLE, PASSWORD_RESET, ROLE_CHANGE
}
